import React from 'react';

const footerStyle = {
  position: 'fixed',
  bottom: '0px',
  textAlign: 'center',
  width: '100%',
};

const Footer = () => {
  return (
    <div style={footerStyle}>
            Open Source Social Production Inc.
    </div >
  );
};

export default Footer;